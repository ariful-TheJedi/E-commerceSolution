<?php

use Illuminate\Support\Facades\Route;

/*
| Modules register their own routes. The host mounts this file and
| nothing else. Module names never appear in URLs.
*/

Route::prefix('v1')->group(function (): void {
    //
});
